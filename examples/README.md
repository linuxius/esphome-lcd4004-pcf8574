# Examples

This folder contains sanitized example YAML files for `lcd4004_pcf8574`.

Files:

- `lcd4004_pcf8574_basic.example.yaml`: basic 40x4 LCD usage.
- `lcd4004_pcf8574_ha_frame_bridge.example.yaml`: Home Assistant text frame
  bridge (4 lines pushed from an `input_text` helper).

Replace placeholders and secrets before deploying.
